const exerciseCalculator = (myList: Array<number>) => {
  return myList[1];
};

const averageMe = (myList: Array<number>): number => {
  return myList.reduce((a, b) => a + b, 0) / myList.length;
};

console.log(exerciseCalculator([1, 2, 3]));
const a = "aeAwef".toUpperCase()
